name: learn-github-actions
on:
  push:
    branches:
      - 'dev'
jobs:
  deploy:
    name: 🚀 Deploy website every commit
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Sync files
        uses: ./
        with:
          server: ftp.kareembh@newtype.solutions
          username: kareembh@newtype.solutions
          password: ${{ secrets.ftp_password }}
          protocol: ftps



